export interface AlertQuery {
  title: string
}

export interface AlertInformation {
  title: string
  type: string
  pushChannel: string
  frequency: string
  description: string
  role: string
  createDate: string
  status: string
}
